#pragma once

namespace Engine
{
    namespace Calc
    {
        float cos(float x);
        float sin(float x);
        float to_radians(float angle);

        unsigned int next_power2(unsigned int v);

        float sqrt(float x);
        float abs(float x);
    }
}
